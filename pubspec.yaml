name: split_it
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.2.0

  #Firebase
  firebase_core: ^1.2.0

  #SocialLogin
  google_sign_in: ^5.2.3

  #Facilitar os testes
  mocktail: ^0.2.0

  #Facilitar os loadings
  shimmer: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
